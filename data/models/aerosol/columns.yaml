# ===================================
#  Column Definitions
# ===================================

columns:

    measurement_id:
        label: ID
        type: numeric

    photometer_id:
        label: Photometer
        relation: photometer
        select: @name
        searchable: true

    value_formatted:
        label: Value

    user_name:
        label: User
        searchable: true

    organization_name:
        label: Organization
        searchable: true

    measured_datetime:
        label: Measured at
        sortable: true